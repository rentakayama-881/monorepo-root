// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend-gin/ent/artifactsubmission"
	"backend-gin/ent/category"
	"backend-gin/ent/consultationrequest"
	"backend-gin/ent/endorsement"
	"backend-gin/ent/finaloffer"
	"backend-gin/ent/predicate"
	"backend-gin/ent/tag"
	"backend-gin/ent/user"
	"backend-gin/ent/validationcase"
	"backend-gin/ent/validationcaselog"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ValidationCaseQuery is the builder for querying ValidationCase entities.
type ValidationCaseQuery struct {
	config
	ctx                      *QueryContext
	order                    []validationcase.OrderOption
	inters                   []Interceptor
	predicates               []predicate.ValidationCase
	withUser                 *UserQuery
	withCategory             *CategoryQuery
	withTags                 *TagQuery
	withCaseLogs             *ValidationCaseLogQuery
	withConsultationRequests *ConsultationRequestQuery
	withFinalOffers          *FinalOfferQuery
	withArtifactSubmissions  *ArtifactSubmissionQuery
	withEndorsements         *EndorsementQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ValidationCaseQuery builder.
func (_q *ValidationCaseQuery) Where(ps ...predicate.ValidationCase) *ValidationCaseQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ValidationCaseQuery) Limit(limit int) *ValidationCaseQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ValidationCaseQuery) Offset(offset int) *ValidationCaseQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ValidationCaseQuery) Unique(unique bool) *ValidationCaseQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ValidationCaseQuery) Order(o ...validationcase.OrderOption) *ValidationCaseQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryUser chains the current query on the "user" edge.
func (_q *ValidationCaseQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(validationcase.Table, validationcase.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, validationcase.UserTable, validationcase.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCategory chains the current query on the "category" edge.
func (_q *ValidationCaseQuery) QueryCategory() *CategoryQuery {
	query := (&CategoryClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(validationcase.Table, validationcase.FieldID, selector),
			sqlgraph.To(category.Table, category.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, validationcase.CategoryTable, validationcase.CategoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTags chains the current query on the "tags" edge.
func (_q *ValidationCaseQuery) QueryTags() *TagQuery {
	query := (&TagClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(validationcase.Table, validationcase.FieldID, selector),
			sqlgraph.To(tag.Table, tag.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, validationcase.TagsTable, validationcase.TagsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCaseLogs chains the current query on the "case_logs" edge.
func (_q *ValidationCaseQuery) QueryCaseLogs() *ValidationCaseLogQuery {
	query := (&ValidationCaseLogClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(validationcase.Table, validationcase.FieldID, selector),
			sqlgraph.To(validationcaselog.Table, validationcaselog.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, validationcase.CaseLogsTable, validationcase.CaseLogsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryConsultationRequests chains the current query on the "consultation_requests" edge.
func (_q *ValidationCaseQuery) QueryConsultationRequests() *ConsultationRequestQuery {
	query := (&ConsultationRequestClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(validationcase.Table, validationcase.FieldID, selector),
			sqlgraph.To(consultationrequest.Table, consultationrequest.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, validationcase.ConsultationRequestsTable, validationcase.ConsultationRequestsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFinalOffers chains the current query on the "final_offers" edge.
func (_q *ValidationCaseQuery) QueryFinalOffers() *FinalOfferQuery {
	query := (&FinalOfferClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(validationcase.Table, validationcase.FieldID, selector),
			sqlgraph.To(finaloffer.Table, finaloffer.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, validationcase.FinalOffersTable, validationcase.FinalOffersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryArtifactSubmissions chains the current query on the "artifact_submissions" edge.
func (_q *ValidationCaseQuery) QueryArtifactSubmissions() *ArtifactSubmissionQuery {
	query := (&ArtifactSubmissionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(validationcase.Table, validationcase.FieldID, selector),
			sqlgraph.To(artifactsubmission.Table, artifactsubmission.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, validationcase.ArtifactSubmissionsTable, validationcase.ArtifactSubmissionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEndorsements chains the current query on the "endorsements" edge.
func (_q *ValidationCaseQuery) QueryEndorsements() *EndorsementQuery {
	query := (&EndorsementClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(validationcase.Table, validationcase.FieldID, selector),
			sqlgraph.To(endorsement.Table, endorsement.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, validationcase.EndorsementsTable, validationcase.EndorsementsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ValidationCase entity from the query.
// Returns a *NotFoundError when no ValidationCase was found.
func (_q *ValidationCaseQuery) First(ctx context.Context) (*ValidationCase, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{validationcase.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ValidationCaseQuery) FirstX(ctx context.Context) *ValidationCase {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ValidationCase ID from the query.
// Returns a *NotFoundError when no ValidationCase ID was found.
func (_q *ValidationCaseQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{validationcase.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ValidationCaseQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ValidationCase entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ValidationCase entity is found.
// Returns a *NotFoundError when no ValidationCase entities are found.
func (_q *ValidationCaseQuery) Only(ctx context.Context) (*ValidationCase, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{validationcase.Label}
	default:
		return nil, &NotSingularError{validationcase.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ValidationCaseQuery) OnlyX(ctx context.Context) *ValidationCase {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ValidationCase ID in the query.
// Returns a *NotSingularError when more than one ValidationCase ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ValidationCaseQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{validationcase.Label}
	default:
		err = &NotSingularError{validationcase.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ValidationCaseQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ValidationCases.
func (_q *ValidationCaseQuery) All(ctx context.Context) ([]*ValidationCase, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ValidationCase, *ValidationCaseQuery]()
	return withInterceptors[[]*ValidationCase](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ValidationCaseQuery) AllX(ctx context.Context) []*ValidationCase {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ValidationCase IDs.
func (_q *ValidationCaseQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(validationcase.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ValidationCaseQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ValidationCaseQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ValidationCaseQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ValidationCaseQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ValidationCaseQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ValidationCaseQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ValidationCaseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ValidationCaseQuery) Clone() *ValidationCaseQuery {
	if _q == nil {
		return nil
	}
	return &ValidationCaseQuery{
		config:                   _q.config,
		ctx:                      _q.ctx.Clone(),
		order:                    append([]validationcase.OrderOption{}, _q.order...),
		inters:                   append([]Interceptor{}, _q.inters...),
		predicates:               append([]predicate.ValidationCase{}, _q.predicates...),
		withUser:                 _q.withUser.Clone(),
		withCategory:             _q.withCategory.Clone(),
		withTags:                 _q.withTags.Clone(),
		withCaseLogs:             _q.withCaseLogs.Clone(),
		withConsultationRequests: _q.withConsultationRequests.Clone(),
		withFinalOffers:          _q.withFinalOffers.Clone(),
		withArtifactSubmissions:  _q.withArtifactSubmissions.Clone(),
		withEndorsements:         _q.withEndorsements.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ValidationCaseQuery) WithUser(opts ...func(*UserQuery)) *ValidationCaseQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUser = query
	return _q
}

// WithCategory tells the query-builder to eager-load the nodes that are connected to
// the "category" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ValidationCaseQuery) WithCategory(opts ...func(*CategoryQuery)) *ValidationCaseQuery {
	query := (&CategoryClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCategory = query
	return _q
}

// WithTags tells the query-builder to eager-load the nodes that are connected to
// the "tags" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ValidationCaseQuery) WithTags(opts ...func(*TagQuery)) *ValidationCaseQuery {
	query := (&TagClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTags = query
	return _q
}

// WithCaseLogs tells the query-builder to eager-load the nodes that are connected to
// the "case_logs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ValidationCaseQuery) WithCaseLogs(opts ...func(*ValidationCaseLogQuery)) *ValidationCaseQuery {
	query := (&ValidationCaseLogClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCaseLogs = query
	return _q
}

// WithConsultationRequests tells the query-builder to eager-load the nodes that are connected to
// the "consultation_requests" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ValidationCaseQuery) WithConsultationRequests(opts ...func(*ConsultationRequestQuery)) *ValidationCaseQuery {
	query := (&ConsultationRequestClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withConsultationRequests = query
	return _q
}

// WithFinalOffers tells the query-builder to eager-load the nodes that are connected to
// the "final_offers" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ValidationCaseQuery) WithFinalOffers(opts ...func(*FinalOfferQuery)) *ValidationCaseQuery {
	query := (&FinalOfferClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFinalOffers = query
	return _q
}

// WithArtifactSubmissions tells the query-builder to eager-load the nodes that are connected to
// the "artifact_submissions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ValidationCaseQuery) WithArtifactSubmissions(opts ...func(*ArtifactSubmissionQuery)) *ValidationCaseQuery {
	query := (&ArtifactSubmissionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withArtifactSubmissions = query
	return _q
}

// WithEndorsements tells the query-builder to eager-load the nodes that are connected to
// the "endorsements" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ValidationCaseQuery) WithEndorsements(opts ...func(*EndorsementQuery)) *ValidationCaseQuery {
	query := (&EndorsementClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEndorsements = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ValidationCase.Query().
//		GroupBy(validationcase.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ValidationCaseQuery) GroupBy(field string, fields ...string) *ValidationCaseGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ValidationCaseGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = validationcase.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ValidationCase.Query().
//		Select(validationcase.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *ValidationCaseQuery) Select(fields ...string) *ValidationCaseSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ValidationCaseSelect{ValidationCaseQuery: _q}
	sbuild.label = validationcase.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ValidationCaseSelect configured with the given aggregations.
func (_q *ValidationCaseQuery) Aggregate(fns ...AggregateFunc) *ValidationCaseSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ValidationCaseQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !validationcase.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ValidationCaseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ValidationCase, error) {
	var (
		nodes       = []*ValidationCase{}
		_spec       = _q.querySpec()
		loadedTypes = [8]bool{
			_q.withUser != nil,
			_q.withCategory != nil,
			_q.withTags != nil,
			_q.withCaseLogs != nil,
			_q.withConsultationRequests != nil,
			_q.withFinalOffers != nil,
			_q.withArtifactSubmissions != nil,
			_q.withEndorsements != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ValidationCase).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ValidationCase{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withUser; query != nil {
		if err := _q.loadUser(ctx, query, nodes, nil,
			func(n *ValidationCase, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCategory; query != nil {
		if err := _q.loadCategory(ctx, query, nodes, nil,
			func(n *ValidationCase, e *Category) { n.Edges.Category = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTags; query != nil {
		if err := _q.loadTags(ctx, query, nodes,
			func(n *ValidationCase) { n.Edges.Tags = []*Tag{} },
			func(n *ValidationCase, e *Tag) { n.Edges.Tags = append(n.Edges.Tags, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCaseLogs; query != nil {
		if err := _q.loadCaseLogs(ctx, query, nodes,
			func(n *ValidationCase) { n.Edges.CaseLogs = []*ValidationCaseLog{} },
			func(n *ValidationCase, e *ValidationCaseLog) { n.Edges.CaseLogs = append(n.Edges.CaseLogs, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withConsultationRequests; query != nil {
		if err := _q.loadConsultationRequests(ctx, query, nodes,
			func(n *ValidationCase) { n.Edges.ConsultationRequests = []*ConsultationRequest{} },
			func(n *ValidationCase, e *ConsultationRequest) {
				n.Edges.ConsultationRequests = append(n.Edges.ConsultationRequests, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withFinalOffers; query != nil {
		if err := _q.loadFinalOffers(ctx, query, nodes,
			func(n *ValidationCase) { n.Edges.FinalOffers = []*FinalOffer{} },
			func(n *ValidationCase, e *FinalOffer) { n.Edges.FinalOffers = append(n.Edges.FinalOffers, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withArtifactSubmissions; query != nil {
		if err := _q.loadArtifactSubmissions(ctx, query, nodes,
			func(n *ValidationCase) { n.Edges.ArtifactSubmissions = []*ArtifactSubmission{} },
			func(n *ValidationCase, e *ArtifactSubmission) {
				n.Edges.ArtifactSubmissions = append(n.Edges.ArtifactSubmissions, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withEndorsements; query != nil {
		if err := _q.loadEndorsements(ctx, query, nodes,
			func(n *ValidationCase) { n.Edges.Endorsements = []*Endorsement{} },
			func(n *ValidationCase, e *Endorsement) { n.Edges.Endorsements = append(n.Edges.Endorsements, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ValidationCaseQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ValidationCase, init func(*ValidationCase), assign func(*ValidationCase, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ValidationCase)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ValidationCaseQuery) loadCategory(ctx context.Context, query *CategoryQuery, nodes []*ValidationCase, init func(*ValidationCase), assign func(*ValidationCase, *Category)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ValidationCase)
	for i := range nodes {
		fk := nodes[i].CategoryID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(category.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "category_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ValidationCaseQuery) loadTags(ctx context.Context, query *TagQuery, nodes []*ValidationCase, init func(*ValidationCase), assign func(*ValidationCase, *Tag)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*ValidationCase)
	nids := make(map[int]map[*ValidationCase]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(validationcase.TagsTable)
		s.Join(joinT).On(s.C(tag.FieldID), joinT.C(validationcase.TagsPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(validationcase.TagsPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(validationcase.TagsPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*ValidationCase]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Tag](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "tags" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *ValidationCaseQuery) loadCaseLogs(ctx context.Context, query *ValidationCaseLogQuery, nodes []*ValidationCase, init func(*ValidationCase), assign func(*ValidationCase, *ValidationCaseLog)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*ValidationCase)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(validationcaselog.FieldValidationCaseID)
	}
	query.Where(predicate.ValidationCaseLog(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(validationcase.CaseLogsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ValidationCaseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "validation_case_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ValidationCaseQuery) loadConsultationRequests(ctx context.Context, query *ConsultationRequestQuery, nodes []*ValidationCase, init func(*ValidationCase), assign func(*ValidationCase, *ConsultationRequest)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*ValidationCase)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(consultationrequest.FieldValidationCaseID)
	}
	query.Where(predicate.ConsultationRequest(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(validationcase.ConsultationRequestsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ValidationCaseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "validation_case_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ValidationCaseQuery) loadFinalOffers(ctx context.Context, query *FinalOfferQuery, nodes []*ValidationCase, init func(*ValidationCase), assign func(*ValidationCase, *FinalOffer)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*ValidationCase)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(finaloffer.FieldValidationCaseID)
	}
	query.Where(predicate.FinalOffer(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(validationcase.FinalOffersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ValidationCaseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "validation_case_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ValidationCaseQuery) loadArtifactSubmissions(ctx context.Context, query *ArtifactSubmissionQuery, nodes []*ValidationCase, init func(*ValidationCase), assign func(*ValidationCase, *ArtifactSubmission)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*ValidationCase)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(artifactsubmission.FieldValidationCaseID)
	}
	query.Where(predicate.ArtifactSubmission(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(validationcase.ArtifactSubmissionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ValidationCaseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "validation_case_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ValidationCaseQuery) loadEndorsements(ctx context.Context, query *EndorsementQuery, nodes []*ValidationCase, init func(*ValidationCase), assign func(*ValidationCase, *Endorsement)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*ValidationCase)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(endorsement.FieldValidationCaseID)
	}
	query.Where(predicate.Endorsement(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(validationcase.EndorsementsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ValidationCaseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "validation_case_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ValidationCaseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ValidationCaseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(validationcase.Table, validationcase.Columns, sqlgraph.NewFieldSpec(validationcase.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, validationcase.FieldID)
		for i := range fields {
			if fields[i] != validationcase.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withUser != nil {
			_spec.Node.AddColumnOnce(validationcase.FieldUserID)
		}
		if _q.withCategory != nil {
			_spec.Node.AddColumnOnce(validationcase.FieldCategoryID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ValidationCaseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(validationcase.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = validationcase.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ValidationCaseGroupBy is the group-by builder for ValidationCase entities.
type ValidationCaseGroupBy struct {
	selector
	build *ValidationCaseQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ValidationCaseGroupBy) Aggregate(fns ...AggregateFunc) *ValidationCaseGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ValidationCaseGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ValidationCaseQuery, *ValidationCaseGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ValidationCaseGroupBy) sqlScan(ctx context.Context, root *ValidationCaseQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ValidationCaseSelect is the builder for selecting fields of ValidationCase entities.
type ValidationCaseSelect struct {
	*ValidationCaseQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ValidationCaseSelect) Aggregate(fns ...AggregateFunc) *ValidationCaseSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ValidationCaseSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ValidationCaseQuery, *ValidationCaseSelect](ctx, _s.ValidationCaseQuery, _s, _s.inters, v)
}

func (_s *ValidationCaseSelect) sqlScan(ctx context.Context, root *ValidationCaseQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
