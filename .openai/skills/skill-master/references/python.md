# Python

## Detection signals
- `pyproject.toml`, `requirements.txt`
- `setup.py`, `Pipfile`

## Source roots
- `src/`, package dirs, `tests/`

## Pattern indicators
- `FastAPI`, `Flask`, `Django`
- `pydantic` models
- repository/service layering
- `pytest` tests

## Key paths
- `app/` or `src/`
- `tests/`
- `alembic/` or `migrations/`
