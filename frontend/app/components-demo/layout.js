export const metadata = {
  title: "Preview Komponen Internal",
  description: "Sandbox internal untuk meninjau komponen UI sebelum dipakai di halaman produksi.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ComponentsDemoLayout({ children }) {
  return children;
}
