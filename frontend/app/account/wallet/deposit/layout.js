export const metadata = {
  title: "Deposit",
  description: "Isi saldo dompet AIValid Anda melalui pembayaran kripto.",
  robots: { index: false },
};

export default function DepositLayout({ children }) {
  return children;
}
