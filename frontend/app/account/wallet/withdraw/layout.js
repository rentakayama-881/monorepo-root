export const metadata = {
  title: "Penarikan",
  description: "Tarik saldo dompet AIValid ke alamat kripto Anda.",
  robots: { index: false },
};

export default function WithdrawLayout({ children }) {
  return children;
}
