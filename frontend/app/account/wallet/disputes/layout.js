export const metadata = {
  title: "Sengketa Transaksi",
  description: "Kelola dan pantau sengketa transaksi Anda di AIValid.",
  robots: { index: false },
};

export default function DisputesLayout({ children }) {
  return children;
}
