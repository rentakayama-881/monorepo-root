/**
 * Re-export shared normalizers for wallet dispute detail.
 */
export {
  normalizeCurrentUser,
  normalizeDisputeMessage,
  normalizeDisputeEvidence,
  normalizeDispute,
} from "@/lib/disputes/normalizers";
