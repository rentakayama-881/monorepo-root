export const metadata = {
  title: "Dompet",
  description: "Kelola saldo, deposit, dan penarikan dompet AIValid Anda.",
  robots: { index: false },
};

export default function WalletLayout({ children }) {
  return children;
}
