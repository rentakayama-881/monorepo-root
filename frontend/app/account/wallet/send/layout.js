export const metadata = {
  title: "Kirim Dana",
  description: "Transfer saldo ke pengguna AIValid lainnya.",
  robots: { index: false },
};

export default function SendLayout({ children }) {
  return children;
}
