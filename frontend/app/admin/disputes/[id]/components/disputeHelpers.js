/**
 * Re-export shared dispute helpers for admin dispute detail.
 */
export {
  formatDate,
  formatAmount,
  normalizeStatus,
  getStatusColor,
  getStatusLabel,
  getCategoryLabel,
} from "@/lib/disputes/helpers";
