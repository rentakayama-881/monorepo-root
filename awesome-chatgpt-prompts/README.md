# awesome-chatgpt-prompts

This directory is kept as a placeholder because the repository previously referenced a git submodule
without a `.gitmodules` entry, which caused submodule fetch warnings.

If you want to restore the original submodule, re-add it with a valid `.gitmodules` entry and a
reachable URL, then run `git submodule update --init`.
